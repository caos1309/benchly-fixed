platform :ios, '12.0'

ENV['COCOAPODS_DISABLE_STATS'] = 'true'

flutter_application_path = File.expand_path('..')
load File.join(flutter_application_path, '.flutter-plugins-dependencies')

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  # Aquí irían los pods si tuvieras plugins con dependencias nativas

  # Eliminamos el post_install que causaba error
end
